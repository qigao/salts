# CFlow Optional USB Adapter Implementation Plan

**Goal:** Deliver issue #114 as the default-off `Salts::CFlowUSB` target.

**Spec:** `docs/superpowers/specs/2026-08-26-cflow-usb-design.md`

### Task 1: Dependency and public contract

- [x] Add vcpkg manifest feature `usb`, default-off CMake option, fail-fast
  pkg-config discovery, separate export target, and default-build absence test.
- [x] Add C/C++ compile-contract tests for context, identity, enumeration,
  handle, transfers, hotplug events, statistics, drive, close, and destroy.

### Task 2: Adapter state machine

- [x] Add a test-private fake libusb implementation behind the native API.
- [x] Write RED lifecycle tests, then implement checked fixed transfer/event
  storage, event-thread ownership, enumeration cleanup, handle/interface state,
  terminal callback publication, cancellation, loss, and shutdown.

### Task 3: Real libusb bridge and verification

- [x] Implement enumeration, open/close, claim/release, control/bulk/interrupt
  transfer submission, hotplug registration, capability checks, and error maps.
- [x] Verify default and USB-enabled configure/build/install paths, run
  hardware-independent tests, and document opt-in physical-device validation.
