name: CNet direct-dispatch lifecycle GREEN edit

on:
  push:
    branches:
      - perf/cnet-dispatch-bypass-276
    paths:
      - .github/workflows/cnet-direct-dispatch-green1-edit.yml

permissions:
  contents: write

jobs:
  edit:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
        with:
          ref: perf/cnet-dispatch-bypass-276
      - name: Apply A1 lifecycle GREEN
        run: |
          python3 .github/scripts/cnet_direct_dispatch_green1_edit.py
          git diff --check
          git diff -- cnet/src/cnet_client.c
      - name: Commit A1 lifecycle GREEN
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add cnet/src/cnet_client.c
          git commit -m 'feat(cnet): add diagnostic direct-dispatch lifecycle'
          git push origin HEAD:perf/cnet-dispatch-bypass-276
