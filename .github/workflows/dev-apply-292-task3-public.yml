name: dev apply cnet slice 292 task3 public
on:
  push:
    branches:
      - perf/cnet-zero-copy-slice-292
    paths:
      - .github/dev-task3-public.py
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: perf/cnet-zero-copy-slice-292
          fetch-depth: 0
      - name: Apply fail-closed Task 3 public test edit
        shell: bash
        run: |
          set -euo pipefail
          python .github/dev-task3-public.py
          rm .github/dev-task3-public.py .github/workflows/dev-apply-292-task3-public.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add cnet/tests/cnet_send_buffer_api_test.c .github/dev-task3-public.py .github/workflows/dev-apply-292-task3-public.yml
          git commit --amend --no-edit
          git push --force-with-lease origin HEAD:perf/cnet-zero-copy-slice-292
